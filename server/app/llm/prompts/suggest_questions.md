Propose three short questions a person could ask about this collection of documents.

# The documents

{document_count} documents. Field statistics:

{stats}

# Example extracted records

{digests}

# What to return

Exactly three questions, each under twelve words.

- One should be **quantitative**: a total, a count, or a comparison across a category that
  the statistics show is actually populated.
- One should be about **specific content** in the documents, the kind of thing that needs a
  passage rather than a number.
- One should be **comparative or cross-document**, something no single document answers.

Only propose a question the fields above can actually answer. A suggestion that returns "not
in these documents" is worse than no suggestion, because the user trusted it.
